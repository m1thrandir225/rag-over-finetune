services:
  qdrant:
    image: qdrant/qdrant:latest
    ports:
      - 6333:6333
    environment:
      - QDRANT_API_KEY=${QDRANT_API_KEY}
    volumes:
      - qdrant_data:/qdrant/data

volumes:
  qdrant_data: